import 'package:animated_splash/utils/types.dart';
import 'package:flutter/material.dart';
const defaultLogoSize = Size(200.0, 200.0);
const defaultBackgroundColor = Colors.white;
const defaultCircularRevealColor = Colors.greenAccent;
const defaultAnimationStyle = AnimationStyles.FadeIn;
const defaultAnimationDuration = Duration(milliseconds: 1500);
const defaultAnimationCurve = Curves.easeInOut;
